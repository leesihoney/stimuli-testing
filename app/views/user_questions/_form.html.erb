<%= form_with(model: user_question, local: true) do |form| %>
  <% if user_question.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_question.errors.count, "error") %> prohibited this user_question from being saved:</h2>

      <ul>
      <% user_question.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :recipient_choice %>
    <%= form.text_field :recipient_choice %>
  </div>

  <div class="field">
    <%= form.label :question_num %>
    <%= form.number_field :question_num %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
